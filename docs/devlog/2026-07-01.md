# DevLog 2026-07-01

> Author：RAG 团队
> Related：docs/release/V1_0_0_READINESS_2026-07-01.md, docs/BACKLOG.md, CHANGELOG.md

## 1. Done（今日/本周完成）

- 完成 B-155：`webapp/` 全量重组至 `backend/`（api/routes/domain/storage 子包），迁移 import 路径、测试和文档引用，不改 API 契约与 SQLite schema
- 走完 v1.0.0 发布门禁：依赖审计、pytest（428 通过）、Vitest（27 通过）、Playwright E2E、文档一致性检查
- 手工冒烟：项目创建 → 目录同步导入 → 检索 → 问答（含来源）→ Markdown/PDF 导出 → 刷新恢复聊天记录 → 负向路径（跨项目导出拒绝、空搜索无假数据）
- Windows 本机 `tauri:build:windows` 生成 NSIS installer；GitHub Actions `Tauri Packaging` workflow（run 28509002224）生成 macOS `.dmg` 与 Linux `.AppImage`
- 判定 v1.0.0 发布门禁为 Go，证据记录进 `docs/release/V1_0_0_READINESS_2026-07-01.md §5`

## 2. Issues（遇到的问题）

- **问题**：本机 `pip-audit` 在默认 GBK codepage 下解析 `requirements.txt` 报 `UnicodeDecodeError`
  **影响**：仅本机命令行调用受影响，CI 环境不受影响
  **当前处理**：执行前设置 `$env:PYTHONUTF8 = "1"`（已写入 readiness 文档 §3.1）
  **跟踪**：无需新建 BACKLOG 项，已在回归脚本中固定
- **问题**：`npm run test:unit` 默认 `pool=forks` 在本机 Windows 环境下 worker 进程崩溃
  **影响**：仅本机手工执行受影响，只运行 3/6 测试文件
  **当前处理**：改用 `--pool=threads` 复测，6 文件 27 测试全过；不影响 CI（Linux runner）
  **跟踪**：无需新建 BACKLOG 项，环境问题非代码缺陷

## 3. Decisions（临时决策）

- 手工冒烟使用隔离 `KI_DB_PATH` / `KI_OUTPUT_DIR`（`tmp/release-v1.0.0/`），避免污染本机默认数据 → 原因：遵循 readiness 文档 §4 建议

## 4. Next（下一步）

- 提交 `chore: release v1.0.0` 并打 tag `v1.0.0`
- 视需要发布 GitHub Release 并附上三平台安装包
