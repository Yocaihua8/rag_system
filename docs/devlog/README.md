# DevLog 开发日志

> 状态：Active
> Owner：RAG 团队
> Last Updated：2026-05-18

本目录用于按日/按周记录开发过程日志，建议文件名使用：

- `YYYY-MM-DD.md`（日报）
- `YYYY-Www.md`（周报）

根目录仍保留 `docs/DEVLOG.md` 作为历史聚合记录，新的持续开发记录逐步迁移到此目录。

## 记录原则

- 每条日志包含：做了什么、卡点、下一步。
- 决策类问题同步回填 `design/` 或 `adr/`，本目录保留过程状态。
- 重大可发布行为变更同步更新 `CHANGELOG.md`。
