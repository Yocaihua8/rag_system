# DevLog 2026-08-03

> Author：Codex
> Iteration：v3 Agent 重构 / B-174
> Related：`../../../plans/B-174-desktop-runtime-foundation.md`、`../../../guides/v3-upgrade.md`、`../../../adr/ADR-010-runtime-separation.md`

## 1. Done

- 检查当前 Git、BACKLOG、活动 plan、v3 能力矩阵、Tauri sidecar、认证中间件和 v3 数据库生命周期。
- 按用户“继续完成后端”启动 B-174；B-175 工程保持原提交并转为等待高保真验收的 Interrupted/blocked。
- 查询当前 Tauri 2 官方 sidecar、managed state 和 capability 文档，并核对本机 crate 的 sidecar `env` API；令牌不通过进程参数传递。
- 新增 ADR-018，冻结默认关闭的 desktop mode、动态 loopback 端口、64 位十六进制进程期令牌、专用 Header 和 Web API Key/JWT 分界。
- 实现后端默认关闭的 desktop mode、精确 loopback/显式端口配置、`X-KI-Desktop-Token` 中间件与 CORS Header。
- 新增后端及集成测试，验证 Web 认证兼容、桌面 health/API/docs 保护、Web Key 不可替代、Obsidian 自认证和预检请求。
- 更新服务入口仓库契约并完成本轮全量验证：Python 后端、集成和仓库测试 688 项通过，三项文档门禁通过。
- 实现默认关闭的 Tauri 安全 sidecar 路径：OS 随机令牌、loopback 临时端口、子进程环境注入、main-only 内存 bootstrap 和异常退出清理。
- 保持 capability 权限为空，不向 WebView 开放通用 shell/environment；Rust 3 项和 Tauri 仓库契约 7 项通过。
- 新增只读 v3 存储预检 API，检查 v2/v3 路径隔离、目标类型/空目录、源可读性、父目录可写性和保守空间余量；预检不创建目标。
- 同步确定性 OpenAPI JSON/TypeScript 类型；存储、API、认证、运行配置与仓库门禁 35 项通过。Windows 当前进程缺少目录符号链接权限，对应动态用例 1 项跳过，其余路径解析和拒绝逻辑已执行。
- 新增幂等 v3 在线备份 API；活动 WAL 数据通过 SQLite backup 生成自包含快照，manifest/database 双 hash、integrity、代际与 revision 验证后原子发布。
- 新增默认 7 份的受管保留策略，只删除可完整复验的旧备份；未知和损坏目录保留。备份阶段门禁 42 项通过，1 项沿用目录 symlink 权限跳过；TypeScript 类型检查和三项文档门禁通过。

## 2. Issues

- **问题**：当前正式 Tauri/Vue 使用固定 8765 且前端没有桌面 bootstrap 接线。
  **影响**：若立即强制桌面令牌或随机端口，会破坏现有正式桌面调用。
  **当前处理**：B-174 新能力默认关闭并独立验收；正式启用留到 React/Tauri 切换门禁。
  **跟踪**：B-174、B-177

## 3. Decisions

- B-174 先建立默认关闭的后端 desktop mode，再接 Tauri 和数据运维；不把固定端口兼容行为误写成目标完成。
- 进程期桌面令牌只允许通过父进程环境和 Tauri 内存状态传递，不进入 CLI、日志、SQLite 或浏览器持久存储。
- 关联 ADR：ADR-018。
- 桌面进程令牌不是 Bearer/JWT；其 401 响应不发布 `WWW-Authenticate: Bearer`，避免客户端走错认证流程。
- 当前 Vue/CSP 仍固定 8765，安全 sidecar 必须保持 opt-in；动态 CSP 与生产入口切换不能在本阶段提前落地。
- 存储预检存在固有 TOCTOU，返回结果不是迁移授权或锁；复制、切换和恢复前必须重新执行同等校验。
- Python sqlite3 connection context 只管理事务、不自动关闭句柄；备份实现必须显式 closing，否则 Windows 会锁住 staging 数据库并阻断失败清理。

## 4. Next

- 实现受控停机恢复、失败回滚与恢复后的代际/revision 复验。

## 5. Links

- Commit：`06450b4`（ADR）、`e6b511a`（后端 desktop mode）、`3ca47c2`（Tauri 安全 sidecar）、`86db67b`（存储预检）；在线备份待提交。
- PR：N/A，本轮不推送或创建 PR。
- Issue：B-174。
- 关联文档：`../../../plans/B-174-desktop-runtime-foundation.md`。
