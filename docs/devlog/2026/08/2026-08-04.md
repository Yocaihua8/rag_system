# DevLog 2026-08-04

> Author：Codex
> Iteration：v3 Agent 重构 / B-180
> Related：`../../../features/agent-tasks-and-runs.md`、`../../../features/project-sources.md`、`../../../design/v3-workflow-execution-contract.md`

## 1. Done

- 修复执行器只在启动时恢复过期 lease 的缺口：维护循环同时持续执行 lease recovery 和审批过期处理。
- 截断或存在读取失败的 Sources 扫描不再删除未出现在本轮结果中的既有 Documents；完整扫描仍会删除已确认缺失的资料。
- 受限已发布工作流的已验证节点配置现冻结到对应 Run Step 输入；同一 Task 存在未终态 Run 时拒绝新的不同幂等 Run。
- 定向 Python 46 项、后端与集成 Python 571 项（1 项 Windows 符号链接权限跳过）、仓库契约 153 项、React v3 48 项单测、类型检查和生产构建均通过。

## 2. Issues

- **问题**：首次全量 Python 命令在约 192 秒时被运行环境异常中断，未产生断言失败或最终汇总。
  **影响**：该次执行不能作为验证结论。
  **当前处理**：按官方范围拆分为后端/集成与仓库契约后均完整通过。
  **跟踪**：N/A

## 3. Decisions

- 单 Task 的“活动”状态定义为全部非终态 Run，包括 queued、running、waiting_approval、paused 和 recovering → 原因：这些状态都可继续改变共享 Task 状态，不能与新 Run 并发。
- 不完整扫描的判定仅基于截断或读取失败 → 原因：这两类情形无法证明既有相对路径已不存在；完整扫描继续保持真实删除语义。

## 4. Next

- 当前没有活动 plan；后续 v3 行为变化须重新建立 B-ID 和独立门禁。

## 5. Links

- PR：N/A，本轮未推送或创建 PR。
- Issue：B-180（完成后从 BACKLOG 移除）。
- 关联文档：`../../features/agent-tasks-and-runs.md`、`../../features/project-sources.md`、`../../design/v3-workflow-execution-contract.md`。
