blank_issues_enabled: false
# Add contact_links only after real, publicly reachable security/support URLs are confirmed.
# Do not ship placeholder or example.com links: GitHub treats them as live navigation.
