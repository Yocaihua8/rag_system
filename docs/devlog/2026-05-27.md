# DevLog 2026-05-27

> Author：RAG 团队
> Related：BACKLOG B-141、`frontend/`、`docs/features/frontend-engineering.md`

## 1. Done（今日完成）

- B-141K 第一片：扩展 `frontend/src/api/imports.js`、`DocumentImportPanel` 和 `App.vue`，资料库视图支持当前项目目录导入预检，复用既有 `GET /api/import/preview`；预检只展示可导入数、跳过数和跳过原因，不创建导入批次、不刷新文档列表。
- B-141L 第一片：新增 `frontend/src/api/document-collections.js` 和 `DocumentCollectionPanel`，资料库视图支持文档集合只读筛选，复用既有 `GET /api/document-collections` 与 `GET /api/documents?collection_id=...`；当前不提供集合新建、删除、加入或移出文档入口。
- B-141M 第一片：扩展 Vue 文档集合 helper、`DocumentCollectionPanel` 和 `App.vue`，资料库视图支持文档集合新建和删除，复用既有 `POST /api/document-collections` 与 `POST /api/document-collections/delete`；删除集合前提示“集合内文档不会被删除”，删除当前筛选集合后回到全部文档。
- B-141N 第一片：扩展 Vue 文档集合 helper、`DocumentCollectionPanel` 和 `App.vue`，资料库视图支持文档集合重命名，复用既有 `POST /api/document-collections/update`；重命名成功后刷新集合列表。
- B-141O 第一片：扩展 Vue 文档集合 helper、`DocumentListPanel` 和 `App.vue`，资料库文档列表支持单文档加入集合和从当前集合移出，复用既有 `POST /api/document-collections/items/add` 与 `POST /api/document-collections/items/remove`；成功后刷新集合列表和当前文档列表。
- B-141P 第一片：扩展 Vue 文档 helper、`DocumentListPanel` 和 `App.vue`，资料库文档列表支持单文档删除，复用既有 `POST /api/documents/delete`；删除前提示源文件不会被删除，成功后刷新文档列表和文档集合列表。
- B-141Q 第一片：扩展 Vue 项目空间 helper、`ProjectSpacePanel` 和 `App.vue`，资料库项目空间面板支持当前项目改名和删除，复用既有 `POST /api/projects/rename` 与 `POST /api/projects/delete`；删除前提示项目内文档记录也会被删除，成功后清空当前项目相关资料库状态。
- B-141R 第一片：新增 Vue 设置页 helper，扩展 `SettingsView` 和 `App.vue`，设置视图支持基础模型设置读取/保存/测试，以及模型 Profile 新增/编辑、删除、默认选择和连接测试；复用既有 `/api/settings/llm` 与 `/api/model-profiles*` 契约，API Key 不回显明文。

## 2. Issues

- 暂无新增问题。

## 3. Decisions

- B-141K 只迁移 Vue 侧预检入口，不改后端 API 和数据库 schema；原因是后端只读预检契约已存在，本片目标是补齐 Vue 资料库导入区域。
- B-141L 先迁移集合列表与筛选，不迁移集合 CRUD 和文档加入/移出；原因是筛选复用现有 `collection_id` 文档列表契约，风险低且能先补齐资料库浏览体验。
- B-141M 只迁移集合新建/删除，不迁移集合重命名、加入文档或移出文档；原因是新建/删除可独立复用既有接口，加入/移出需要改文档列表项操作区，适合后续单独切片。
- B-141N 只迁移集合重命名，不迁移文档加入/移出集合；原因是重命名只影响集合元数据和集合列表刷新，加入/移出需要改文档列表项操作区，适合后续单独切片。
- B-141O 只迁移单文档加入/移出集合，不迁移批量选择、拖拽、删除文档或问答按集合过滤；原因是 items add/remove 已有后端契约，本片可限定在文档列表项操作区和刷新流。
- B-141P 只迁移单文档删除，不迁移批量删除、源文件删除、项目改名/删除或问答按集合过滤；原因是 `POST /api/documents/delete` 已有后端契约，本片可限定在文档列表项操作区和刷新流。
- B-141Q 只迁移项目空间改名/删除，不迁移项目根目录修改、批量项目管理、备份恢复、Workbench SSE/会话或设置页模型配置；原因是项目改名/删除已有后端契约，本片可限定在 `ProjectSpacePanel` 和 App 状态清理流。
- B-141R 只迁移设置页基础模型设置和模型 Profile，不迁移 Prompt 预设、Workbench SSE/会话、模型配置导入导出或数据库 schema；原因是模型设置/Profile 后端契约已存在，本片可限定在 Vue 设置视图和 App 状态流。
- 技术栈迁移整体完成前不推送；本片按计划继续本地提交。

## 4. Next

- B-141 后续可继续迁移工作台 SSE/会话、设置页 Prompt 预设或评估页。

## 5. Links

- 关联文档：`docs/features/frontend-engineering.md`、`docs/design/architecture-overview.md`、`docs/guides/testing.md`

## 6. Verification

- B-141K 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 28 passed，失败原因为缺少 `previewProjectImport`、导入面板预检入口和 App 预检状态接入。
- B-141K 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，31 passed。
- B-141K 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141K Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，302 passed。
- B-141K Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141K 浏览器烟测：打开 `http://127.0.0.1:8765/`，切换到“资料库”，确认“预检当前项目目录 / 同步当前项目目录 / 选择本机文件夹导入 / 选择文件上传导入 / 导入批次历史 / 文档列表 / 文档预览”可见，控制台 error 数 0。
- B-141L 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 31 passed，失败原因为缺少 `document-collections.js`、`DocumentCollectionPanel.vue` 和 App 集合状态接入。
- B-141L 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，34 passed。
- B-141L 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141L Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，305 passed。
- B-141L Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141L 浏览器烟测：打开 `http://127.0.0.1:8765/`，切换到“资料库”，确认“文档集合 / 全部文档 / 未分组 / 刷新集合 / 文档列表 / 文档预览 / 导入资料 / 导入批次历史”可见，控制台 error 数 0。
- B-141M 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 31 passed，失败原因为缺少集合 create/delete API helper、`DocumentCollectionPanel` 新建/删除入口和 App 集合管理状态接入。
- B-141M 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，34 passed。
- B-141M 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141M Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，305 passed。
- B-141M Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141M 文档契约：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_docs_contract.py -q`，22 passed。
- B-141M 浏览器烟测：打开 `http://127.0.0.1:8765/`，切换到“资料库”，确认“B-141C 至 B-141M / 文档集合 / 新建集合 / 集合名称 / 创建集合 / 全部文档 / 未分组 / 刷新集合”可见，创建集合按钮可用，控制台 error 数 0。
- B-141N 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 31 passed，失败原因为缺少 `updateDocumentCollection`、集合面板重命名入口和 App 重命名状态接入。
- B-141N 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，34 passed。
- B-141N 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141N Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，305 passed。
- B-141N Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141N 文档契约：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py tests\test_webapp\test_docs_contract.py -q`，56 passed。
- B-141N 浏览器烟测：打开 `http://127.0.0.1:8765/`，切换到“资料库”，通过 UI 新建临时集合 `B141N Smoke ...`、点击“重命名集合”、输入新名称并保存，确认“文档集合已重命名”和重命名后的集合项可见，控制台 error 数 0；烟测后通过既有删除接口清理临时集合。
- B-141O 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，4 failed / 33 passed，失败原因为缺少集合 items add/remove helper、文档列表加入/移出入口和 App 处理函数。
- B-141O 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，37 passed。
- B-141O 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141O Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，308 passed。
- B-141O Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141O 文档契约：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_docs_contract.py -q`，22 passed。
- B-141O 浏览器烟测：打开 `http://127.0.0.1:8765/`，创建临时项目/文本笔记/文档集合，切换到“资料库”，选择临时项目后确认“加入集合”入口可见；选择集合并点击“加入集合”后集合计数变为 1 且显示“文档已加入集合”；切换集合筛选后点击“移出集合”，集合计数回到 0 且显示“文档已从集合移出”；控制台 error 数 0；烟测后通过既有项目删除接口清理临时项目。
- B-141P 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，4 failed / 36 passed，失败原因为缺少文档 delete helper、文档列表删除入口、App 删除状态流和资料库迁移文案更新。
- B-141P 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，40 passed。
- B-141P 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141P Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，311 passed。
- B-141P Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141P 文档契约：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_docs_contract.py -q`，22 passed。
- B-141P 浏览器烟测：启动本地服务后创建临时项目和文本笔记，使用独立 headless Edge 打开 `http://127.0.0.1:8765/`，切换“资料库”，选择临时项目，确认“删除文档”入口可见；点击删除时捕获确认文案“确认从当前项目空间移除这个文档记录？源文件不会被删除。”，确认后页面显示“文档已删除”，接口侧剩余文档数为 0；CDP 未捕获 Runtime exception 或 `console.error`，仅捕获 1 条 `favicon.ico` 404 resource log；烟测后通过既有项目删除接口清理临时项目。
- B-141Q 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 40 passed，失败原因为缺少项目 rename/delete helper、项目空间面板改名/删除入口和 App 处理状态流。
- B-141Q 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，43 passed。
- B-141Q 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
- B-141Q Web MVP 全量：`.venv\Scripts\python.exe -m pytest tests\test_webapp -q`，314 passed。
- B-141Q Legacy 回归：`.venv\Scripts\python.exe -m pytest tests\test_application tests\test_domain tests\test_adapters -q`，179 passed。
- B-141Q 浏览器烟测：启动本地服务后创建临时项目 `B141Q Smoke ...`，使用 Codex in-app browser 打开 `http://127.0.0.1:8765/`，切换“资料库”，确认临时项目、`重命名项目空间`、`保存项目名称`、`删除项目空间` 和“项目内文档记录也会被删除”提示可见；控制台 error 数 0；烟测后通过既有项目删除接口清理临时项目并停止本地服务。
- B-141R 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，3 failed / 43 passed，失败原因为缺少设置页 helper、设置页模型配置 UI 和 App 状态流。
- B-141R 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，46 passed。
- B-141R 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
