# DevLog 2026-08-03

> Author：Codex
> Iteration：v3 Agent 重构 / B-174、B-175、B-178
> Related：`../../../guides/v3-upgrade.md`、`../../../adr/ADR-010-runtime-separation.md`

## 1. Done

- B-179 已完成并关闭：将启动时加载的 `RAG_CHUNK_SIZE`、`RAG_CHUNK_OVERLAP`、`RAG_RETRIEVER_KIND` 与 `RAG_TOP_K` 接入 v2 活动分块和检索链路。新导入、更新及缺失 chunk 回填使用分块参数；新项目以检索参数初始化，已有项目保存设置和已有 chunk 不自动重写。默认搜索、检索复盘及只读 `search_sources` 均使用项目有效设置。定向回归 146 项、全量 Python 721 项（1 项 Windows 符号链接权限跳过）、Vue 105 项、Vue E2E 2 项、生产构建和三项文档门禁均通过。
- 用户明确要求“进入开发阶段”；扫描工作区、BACKLOG 和执行计划后创建 B-176，按 v3 迁移顺序从真实 Sources 资料能力开始。当前只建立任务边界和恢复点，尚未修改业务代码或切换正式入口。
- B-176 首段已冻结 Sources 合同：仅只读扫描用户已绑定项目根内的文本文件，v3 响应不暴露文件正文或绝对路径；现有 v3 Schema 足以承载首段，不迁移或触碰 v2 数据。
- B-176 已完成 Sources 首段：新增 v3 项目根资料扫描、Sources/Documents 元数据 API、生成 OpenAPI 类型和 React 项目页真实展示。重复扫描幂等，变化和删除文件同步 v3 快照；扫描不跟随符号链接、不修改源项目且不返回正文/绝对路径。定向 Python 41 项、React typecheck、44 项单测、OpenAPI 生成和 React build 均通过；浏览器 E2E 留待 B-176 最终联调矩阵。
- B-176 已完成 Project Insights 资料快照概览：新增只读 overview API，基于当前 v3 Documents 元数据计算来源数、文件数、大小、类型、常见清单、稳定 fingerprint 和可追溯证据；React 项目页显示实际资料快照，资料为空时明确提示先扫描。它不读取正文、项目根或 v2 数据，不调用模型、不写入洞察结论。定向 Python 42 项、React typecheck、45 项单测、OpenAPI 生成和 React build 均通过；浏览器 E2E 仍留待 B-176 最终联调矩阵。
- B-176 已完成 v3 模型 Profile 元数据设置：复用独立 v3 `model_profiles` 表新增列表、创建、全量更新、默认选择和确认删除 API；React 设置页接入真实表单与列表。接口和页面仅处理受控 Key 引用，不接收/解析/回显 Key，不调用外部 provider，也不影响 v2 Profile、`.env` 或正式入口。定向 Python 43 项、React typecheck、46 项单测、OpenAPI 生成和 React build 均通过；浏览器 E2E 仍留待 B-176 最终联调矩阵。
- B-176 冻结 Artifact 受控导出前置合同：已完成 Artifact 不能借用旧 Run 的 Approval 行进入伪造等待状态；后续采用预览不写入、确认时固定 hash/version 并独占写入受管 v3 导出目录的两阶段路径。尚未实现代码或开放导出 API。
- B-176 已完成 Artifact 受控导出：新增 ready Artifact 的导出预览和确认 API，确认请求固定 `version/checksum`、要求 `Idempotency-Key`，且服务端仅写入 v3 受管 `artifacts/exports/` 目录。React 详细过程先显示目标文件名、快照大小及不可自动撤销提示，再由用户明确确认；不接受自选路径、不读取项目根或 v2 数据、也不伪造 `artifact.exported` SSE 事件。定向 Python 44 项、React typecheck、47 项单测、OpenAPI 生成、React production build 和三项文档门禁均通过；浏览器 E2E 继续留待 B-176 最终矩阵。
- 导出确认补充中断恢复：若写入受管文件后尚未提交 Artifact 状态即进程终止，后续请求只能复用内容与当前 ready 快照逐字一致的受管文件；目标不匹配、目录或不可读时仍 fail closed。定向导出/API 契约 14 项与三项文档门禁通过。
- 冻结下一阶段的受限工作流执行合同：管理 API 已能保存/发布/绑定 DAG，但运行入口和执行器尚未支持通用图。首段将仅允许已发布且绑定到项目的既有四节点只读/分析图；LLM、正文检索、审批写入、导出、外部发布和控制流一律在创建 Run 前拒绝。
- 完成受限工作流 Run 创建与执行：显式已发布/已绑定的 `workflow_version_id` 可冻结到 Run，四节点图映射为现有持久 Steps 并通过 executor 完成；注册表端口类型同步为该真实执行顺序。定向应用、工作流 API 与 OpenAPI 契约 21 项通过，React 类型生成与 typecheck 通过。
- React 工作流详情仅在当前项目存在 enabled 且版本匹配的 Binding 时显示任务输入与启动入口；提交依次调用真实 Task 和 Run API，并使用 intent 幂等键后跳转到任务页。React typecheck 与 47 项单测通过。
- 完成 v3 Sources/洞察/工作流浏览器闭环：真实 Playwright 创建项目后扫描 Sources、展示资料概览、生成资料事实报告，再创建/发布/绑定受限工作流并从 React 启动；两个 Run 完成后均保持可恢复，3 项 v3 E2E 通过。
- 完成持久资料事实报告：固定 `project.source-facts.v1` 只从独立 v3 Store 的已持久 Documents 读取正文，生成带快照 hash、逐文档非空行数、有限 Markdown 标题和证据的 `project_source_facts` Artifact；不重新访问项目根、不调用 LLM 或 provider。后续 Sources 扫描变更 hash 时只更新旧报告 metadata 的陈旧标记，不改写 Artifact 正文、checksum、Task 或 Run；未新增 Schema/Alembic migration。定向 Python 15 项、React 48 项单测、v3 Playwright 3 项和 React build 通过。
- B-176 已完成并关闭：补齐真实 Playwright 的资料事实报告受管导出、持久待审批记录展示/二次确认/决议，以及临时 `KI_DATA_ROOT` 隔离；全量 Python 716 passed、1 skipped（Windows 符号链接权限），Vue 105 项、Vue E2E 2 项、React v3 48 项、React v3 E2E 4 项、两套前端构建、`cargo check` 与 Windows bundle 均通过。资料事实报告满足本计划的持久分析段；通用持久洞察、模型调用、工作流编辑/绑定和正式入口切换继续留给后续独立 B-ID。
- Sources 首段以 `cdfdedf` 保存为独立开发检查点；未推送、未创建 PR，且未切换 Vue/Tauri/Docker 正式入口。
- 检查当前 Git、BACKLOG、活动 plan、v3 能力矩阵、Tauri sidecar、认证中间件和 v3 数据库生命周期。
- 按用户“继续完成后端”启动 B-174；B-175 工程保持原提交并转为等待高保真验收的 Interrupted/blocked。
- 查询当前 Tauri 2 官方 sidecar、managed state 和 capability 文档，并核对本机 crate 的 sidecar `env` API；令牌不通过进程参数传递。
- 新增 ADR-018，冻结默认关闭的 desktop mode、动态 loopback 端口、64 位十六进制进程期令牌、专用 Header 和 Web API Key/JWT 分界。
- 实现后端默认关闭的 desktop mode、精确 loopback/显式端口配置、`X-KI-Desktop-Token` 中间件与 CORS Header。
- 新增后端及集成测试，验证 Web 认证兼容、桌面 health/API/docs 保护、Web Key 不可替代、Obsidian 自认证和预检请求。
- 更新服务入口仓库契约并完成本轮全量验证：Python 后端、集成和仓库测试 688 项通过，三项文档门禁通过。
- 实现默认关闭的 Tauri 安全 sidecar 路径：OS 随机令牌、loopback 临时端口、子进程环境注入、main-only 内存 bootstrap 和异常退出清理。
- 保持 capability 权限为空，不向 WebView 开放通用 shell/environment；Rust 3 项和 Tauri 仓库契约 7 项通过。
- 新增只读 v3 存储预检 API，检查 v2/v3 路径隔离、目标类型/空目录、源可读性、父目录可写性和保守空间余量；预检不创建目标。
- 同步确定性 OpenAPI JSON/TypeScript 类型；存储、API、认证、运行配置与仓库门禁 35 项通过。Windows 当前进程缺少目录符号链接权限，对应动态用例 1 项跳过，其余路径解析和拒绝逻辑已执行。
- 新增幂等 v3 在线备份 API；活动 WAL 数据通过 SQLite backup 生成自包含快照，manifest/database 双 hash、integrity、代际与 revision 验证后原子发布。
- 新增默认 7 份的受管保留策略，只删除可完整复验的旧备份；未知和损坏目录保留。备份阶段门禁 42 项通过，1 项沿用目录 symlink 权限跳过；TypeScript 类型检查和三项文档门禁通过。
- 建立恢复存储事务：Store checkpoint/关闭后按确认 hash 和 revision 复验，使用同目录 staging 替换；模拟激活失败可放回原 DB/WAL/SHM 并重新初始化。恢复核心 35 项通过，1 项沿用 symlink 权限跳过。
- 完成受控恢复 HTTP 协调：维护门禁覆盖完整 ASGI 响应，存在活动请求时拒绝恢复；恢复窗口内阻止新请求，并协调 executor、WAL checkpoint、Store 关闭/重开和失败回滚。
- 新增恢复确认 SHA-256、`system.restore` 持久幂等回放和公开数据库信息脱敏；重开数据库后同 Key 回放，不同请求 hash 返回冲突。
- 同步 v3 OpenAPI JSON/TypeScript 类型以及 API、数据库、runbook、setup、testing、升级指南；恢复阶段定向测试 23 项通过，1 项沿用 Windows symlink 权限跳过，React v3 类型检查通过。
- 全量 Python 707 项通过、1 项沿用 Windows symlink 权限跳过；Rust 3 项、Vue 105 项、React v3 42 项及两套构建通过。
- 真实 PyInstaller sidecar 前两次启动依次暴露 Alembic migrations 和 `backend/alembic.ini` 未打包：进程均以代码 3 退出，隔离数据已清理。Windows/Unix 构建脚本现显式加入完整 Alembic 文件资源并增加仓库契约，等待重打包复验。
- 重打包后动态冒烟验证通过：随机 loopback 端口、无/错令牌 401、正确令牌 v3 health、运行中 executor 与恢复 OpenAPI 路径均符合合同；精确终止测试进程树并清理隔离目录后无残留。
- 用户明确表示“前端确认验收”；据此完成 React 高保真门禁，回流 UI 简报、功能状态、迁移指南、ADR-017 与 CHANGELOG，并关闭 B-175。未修改前端代码或正式运行入口。
- 用户明确表示“Sites 不用做了”；取消 B-178 唯一未完成的外部发布项，不再查询、创建或部署 Sites 项目。保留已完成的后端消息流、P1 Revision 3、本地验收和历史附件，并关闭 B-178。

## 2. Issues

- **问题**：当前正式 Tauri/Vue 仍使用固定 8765，安全 bootstrap 尚未接入正式页面调用链。
  **影响**：若立即强制桌面令牌或随机端口，会破坏现有正式桌面调用。
  **当前处理**：安全能力已完成并保持默认关闭；正式启用留到 React/Tauri 切换门禁。
  **跟踪**：B-177

## 3. Decisions

- 对 ISSUE-009 采用“已有项目设置优先”的兼容边界：环境变量仅作为新项目初始检索设置和后续分块写入参数，不迁移已有项目设置或自动重分块历史数据。
- B-174 先建立默认关闭的后端 desktop mode，再接 Tauri 和数据运维；不把固定端口兼容行为误写成目标完成。
- 进程期桌面令牌只允许通过父进程环境和 Tauri 内存状态传递，不进入 CLI、日志、SQLite 或浏览器持久存储。
- 关联 ADR：ADR-018。
- 桌面进程令牌不是 Bearer/JWT；其 401 响应不发布 `WWW-Authenticate: Bearer`，避免客户端走错认证流程。
- 当前 Vue/CSP 仍固定 8765，安全 sidecar 必须保持 opt-in；动态 CSP 与生产入口切换不能在本阶段提前落地。
- 存储预检存在固有 TOCTOU，返回结果不是迁移授权或锁；复制、切换和恢复前必须重新执行同等校验。
- Python sqlite3 connection context 只管理事务、不自动关闭句柄；备份实现必须显式 closing，否则 Windows 会锁住 staging 数据库并阻断失败清理。
- 恢复不能直接做成普通同步路由；必须先增加请求排他门禁并协调 executor 停止，否则并发 API 或后台租约可能在 checkpoint/替换窗口继续写入。
- FastAPI 的装饰器 HTTP 中间件在流式响应发送完成前可能已经返回，不能用它统计完整 SSE 生命周期；恢复排他使用原生 ASGI 中间件，直到最后一个响应 body 发完才释放活动请求计数。
- PyInstaller 能分析到 Alembic Python 依赖，但不会自动收集以文件路径加载的 migration 目录和 `backend/alembic.ini`；sidecar 构建必须把二者作为 data 显式打包。
- PyInstaller onefile 在 Windows 上包含引导父进程与解包子进程；动态门禁不能只停止 `Start-Process -PassThru` 返回的 PID，必须在确认没有既有实例后按精确二进制路径清理本次进程树。
- 前端高保真验收确认当前视觉与交互基线；工作流编辑、通用两轮引导、完整业务联调和正式入口切换仍需真实 API 与独立阶段门禁，不能由验收语句自动扩大授权。
- Sites 不是产品运行依赖；用户取消外部展示后，不再把生产 URL 作为 B-178 完成条件，也不删除本地原型或改写 2026-08-02 的实际失败记录。
- 已完成 Artifact 的导出不复用 Run 审批：现有 Approval 绑定运行中的 Step，导出采用独立的“预览后明确确认”合同；后续自选路径、外部发布或工作流写节点仍须另建生命周期与审批边界。
- 资料事实报告的正文读取只发生在 executor 对 Store 的内部调用；Sources/Document 列表、概览 API 和浏览器 ledger 继续不返回完整正文或绝对路径。报告将旧快照标记为陈旧而不改写内容，以保持已完成 Run/Artifact 可审计。

## 4. Next

- B-174、B-175、B-176、B-178、B-179 均已完成并回流；当前没有活动 plan，后续开发需重新建立对应 B-ID 和独立门禁。

## 5. Links

- Commit：`06450b4`（ADR）、`e6b511a`（后端 desktop mode）、`3ca47c2`（Tauri 安全 sidecar）、`86db67b`（存储预检）、`cc5a437`（在线备份）、`aa46e3d`（恢复回滚核心）、`e3b935b`（受控恢复 API）、`008ac08`（sidecar Alembic 资源）。
- PR：N/A，本轮不推送或创建 PR。
- Issue：B-174（完成，已从 BACKLOG 移除）。
- Issue：B-175（完成，已从 BACKLOG 移除）；实现提交 `da4ffde`。
- Issue：B-178（Sites 交付由用户取消，已从 BACKLOG 移除）；核心提交 `661fe11`、`2ca8998`。
- Issue：B-179（ISSUE-009 已修复，已从 BACKLOG 移除）。
