# DevLog 2026-05-24

> Author：RAG 团队
> Related：BACKLOG B-109 ~ B-112、`docs/design/model-profiles-design.md`

## 1. Done（今日完成）

- B-109：项目级 Prompt 设置设计（规划 `prompt_presets` 表、默认预设注入、固定来源约束优先级）
- B-110：项目级 Prompt 设置第一片（新增/编辑/删除预设，设置默认预设；`/api/answer` 注入 `system_prompt` / `answer_format`）
- B-111：模型 Profile 多配置设计（规划多 provider / 多模型 Profile、默认 Profile、Key 引用边界）
- B-112：模型 Profile 多配置第一片（设置页 CRUD + 默认 Profile 选择；`/api/answer` 优先使用默认 Profile）

## 2. Issues

- **问题**：Profile Key 引用格式（`env:xxx` / `saved:xxx`）在前端显示时容易引起用户困惑
  **影响**：用户可能误以为 Key 没有保存
  **当前处理**：设置页已添加说明文案，标注 Key 来源而非明文
  **跟踪**：留意后续用户反馈

## 3. Decisions

- Profile 只保存 Key 引用而非明文 → 原因：安全约束，与 SECURITY.md 中"API Key 不明文持久化"一致
- Prompt 预设不保存 API Key → 原因：预设是纯文本配置，不应混入凭据

## 4. Next

- B-113 文档集合分组模型设计
- B-115 导入批次历史设计
- B-121 Markdown 渲染（ISSUE-001 处理）

## 5. Links

- 关联文档：`docs/design/model-profiles-design.md`、`docs/design/api-spec.md`
