# DevLog 2026-05-23

> Author：RAG 团队
> Related：BACKLOG B-84 ~ B-108、`docs/design/api-spec.md`、`docs/design/database-design.md`

## 1. Done（今日完成）

**v0.8.0 发布**（多会话聊天 + 检索复盘 + Agent 工具面板 + 备份导出）

**UX 改善**
- B-84：模型设置错误提示细分（未配置 Key / 鉴权失败 / 连接失败分类提示）
- B-85：模型设置状态摘要（保存/测试成功后展示服务信息，不回显 Key 明文）
- B-86：模型设置输入提示收口（补充非技术用户说明）
- B-87：资料库导入状态说明收口（跳过→未导入，错误→读取失败）

**可观察性**
- B-88：项目健康概览只读 API（`GET /api/projects/summary`，文档/chunk/向量/聊天统计）
- B-89：项目仪表盘概览第一片（资料库页展示 summary 数据）
- B-90：来源质量与检索健康展示（`source_quality` 字段提示来源充分/偏少/无来源）
- B-104：问答可观察性（`/api/answer` 返回默认检索参数、命中来源数、模式、耗时）
- B-105：项目级检索默认值（`top_k` / 分数阈值 / 关键词向量开关持久化）

**Agent 工具增强**
- B-91：工具运行详情只读接口（`GET /api/agent/tools/runs/detail`，按 `run_id` 返回审计记录）
- B-92：Agent 工具能力说明与运行详情展示（`label` / `use_cases` / 参数摘要展示）
- B-93：导入预检只读接口（`GET /api/import/preview`，预估可导入文件数不写入）
- B-94：检索复盘详情与删除入口（detail + 二次确认删除）
- B-95：聊天记录单条删除与清空（前端二次确认）
- B-96：Agent 工具白名单元数据扩展（`parameters_schema` / `result_summary` / `use_cases`）
- B-97：Agent 工具面板第一片（工作台展示只读工具 schema 和运行按钮）
- B-98：工具结果引用（一键把成功运行的 `run_id` 带入下一轮问答）

**导入能力**
- B-99：单文件上传导入（浏览器文件选择入口，复用 `POST /api/import/upload`）
- B-100：剪贴板文本导入（复用文本笔记导入）
- B-101：URL 摘录占位（`url:` 虚拟来源，第一版不自动抓取）

**备份导出 / 问答增强**
- B-102：备份导出第一片（`GET /api/export/project` 只读返回元数据和聊天记录）
- B-103：备份恢复最小闭环（`POST /api/export/project/restore` 恢复为新项目空间）
- B-106：回答质量反馈（4 个维度，保存到 `answer_feedback`，不调外部）
- B-107：多会话聊天模型设计（`chat_sessions` 表）
- B-108：多会话聊天第一片（新建/切换/改名/删除会话，`/api/answer` 按 `session_id` 隔离）

## 2. Issues

- **问题**：一天完成 25 个 B-xxx 任务，部分接口仅冒烟测试
  **影响**：测试覆盖密度不均
  **当前处理**：B-31 新功能测试补充列入 P1 backlog
  **跟踪**：B-31

## 3. Decisions

- 备份第一版不含 chunk/vector → 原因：导出体积小、恢复快；完整备份见 B-124
- `answer_feedback` 本地保存，不上传外部 → 原因：隐私约束；本地数据够用于自我改进

## 4. Next

- B-109 项目级 Prompt 设置设计
- B-111 模型 Profile 多配置设计

## 5. Links

- 关联文档：`docs/design/api-spec.md`、`docs/design/database-design.md`
- 版本记录：`CHANGELOG.md [v0.8.0]`
