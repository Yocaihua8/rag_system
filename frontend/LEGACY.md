# LEGACY 目录说明

本目录为历史阶段的 Web 前端链路（Vite + Vue），用于旧版桌面壳方案。

当前重构后的主路径为：

- `app/qt/`
- `core/`
- `infra/`

## 使用约束

1. 默认不在本目录继续新增业务功能。
2. 可用于历史实现参考、样式复用、迁移对照。
3. 若需临时修复旧链路问题，应同步登记迁移计划。

## 入口说明

- 当前推荐启动入口不是本目录，而是：
  - `py -3 desktop\main.py`
  - `py -3 -m app.launcher --mode qt`
