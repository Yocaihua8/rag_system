# DevLog 2026-05-28

> Author：RAG 团队
> Related：BACKLOG B-141、`frontend/`、`docs/features/frontend-engineering.md`

## 1. Done（今日完成）

- B-141S 第一片：扩展 Vue 设置页 helper、`SettingsView` 和 `App.vue`，设置视图支持当前项目 Prompt 预设列表、内置模板复制、新建/编辑、删除、设置默认和清空默认；复用既有 `/api/prompt-presets*` 契约，不改后端 API 或数据库 schema。

## 2. Issues

- 暂无新增问题。

## 3. Decisions

- B-141S 只迁移 Vue 侧 Prompt 预设管理入口，不迁移 Workbench SSE/会话、评估页、真实 LLM prompt 注入逻辑或数据库 schema；原因是 `/api/prompt-presets*` 后端契约和真实 LLM 注入行为已存在，本片可限定在设置页 UI 和 App 状态流。
- 技术栈迁移整体完成前不推送；本片按计划继续本地提交。

## 4. Next

- B-141 后续可继续迁移 Workbench SSE/会话或评估页。

## 5. Links

- 关联文档：`docs/features/frontend-engineering.md`、`docs/design/architecture-overview.md`、`docs/guides/testing.md`

## 6. Verification

- B-141S 红灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，4 failed / 45 passed，失败原因为缺少 Prompt 预设 helper、设置页 UI、App 状态流和 B-141S 文案。
- B-141S 聚焦绿灯：`.venv\Scripts\python.exe -m pytest tests\test_webapp\test_frontend_vue_app.py -q`，49 passed。
- B-141S 前端构建：`npm run build`，Vite 构建成功，输出到 `webapp/static_dist/`。
