# DevLog 2026-05-22

> Author：RAG 团队
> Related：BACKLOG B-75 ~ B-83、`docs/design/api-spec.md`

## 1. Done（今日完成）

- B-75：工具来源回填（`/api/answer` 通过 `tool_run_id` 合并工具命中来源，返回 `tool_context`）
- B-76：检索调试接口与工作台诊断区（临时调整 `top_k` / 关键词向量开关，查看 chunk 命中详情）；PDF 可选解析接入 `pymupdf`
- B-77：文本笔记导入（`note:` 虚拟来源，同标题再次导入只更新原记录）
- B-78：检索复盘第一片（保存诊断快照，记录查询参数、命中来源、质量评估和备注）
- B-79：Web MVP 产品边界文档统一（明确 `app.py` 为默认入口，PySide6 为 legacy）
- B-80：首次使用引导闭环（按钮状态、文案与异步禁用）
- B-81：空状态补充下一步动作提示（暂无文件 / 来源 / 检索结果指向资料库或换词）
- B-82：错误提示可恢复化（补充高频失败场景的下一步操作说明）
- B-83：服务不可用错误兜底（本地服务断开 / 非 JSON 响应时返回可恢复提示，不暴露浏览器错误）

## 2. Issues

- **问题**：检索调试参数（top_k / 分数阈值）不持久化，用户每次调试需重新设置
  **影响**：频繁调参体验较差
  **当前处理**：B-105 项目级检索默认值可作为后续持久化方案
  **跟踪**：B-105（下一日）

## 3. Decisions

- 检索复盘只保存快照，不自动与问答绑定 → 原因：手动保存更灵活，避免每次问答都写入快照
- `note:` 虚拟来源同标题更新而非新增 → 原因：防止重复导入污染检索结果

## 4. Next

- B-84 模型设置错误提示细分
- B-88 项目健康概览只读 API
- B-97 Agent 工具面板
- B-102 备份导出
- B-108 多会话聊天

## 5. Links

- 关联文档：`docs/design/api-spec.md`、`docs/design/database-design.md`
